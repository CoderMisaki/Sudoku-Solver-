diff --git a/index.html b/index.html
index afcd405..5baabf5 100644
--- a/index.html
+++ b/index.html
@@ -444,10 +444,16 @@

         function initGrid() {
             applyLanguageToUI();
+
+            // ⚡ Bolt: Optimize DOM manipulation by using DocumentFragment and an array to cache block references
+            const fragment = document.createDocumentFragment();
+            const blocks = [];
+
             for (let i = 0; i < 9; i++) {
                 const block = document.createElement('div');
                 block.className = 'sudoku-block';
-                gridEl.appendChild(block);
+                fragment.appendChild(block);
+                blocks.push(block);
             }
             for (let r = 0; r < 9; r++) {
                 cells[r] = [];
@@ -456,10 +462,13 @@
                     cell.className = 'sudoku-cell';
                     cell.onpointerdown = () => selectCell(r, c, cell);
                     const blockIdx = Math.floor(r / 3) * 3 + Math.floor(c / 3);
-                    gridEl.children[blockIdx].appendChild(cell);
+                    blocks[blockIdx].appendChild(cell);
                     cells[r][c] = cell;
                 }
             }
+
+            gridEl.appendChild(fragment);
+
             loadState();
             selectCell(4, 4, cells[4][4]);
         }
